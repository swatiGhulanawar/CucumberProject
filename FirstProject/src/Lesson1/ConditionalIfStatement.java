package Lesson1;

public class ConditionalIfStatement {
	public static void main(String[] args) {
		/*
		 * conditional if statement means which evaluate some condition based on true/false and 
		 * returns the output based on the operators
		 * == equals too
		 * != Not equals too
		 * > greater than
		 * <less than
		 * >= greater than equals to
		 * <= less than equals to
		 * 
		 */
		int x=1;
		if ( x==10) { 
			System.out.println("Correct match of the values");
		}else {
			System.out.println("value of x dosent match with value:" +x);

			}
		
	}

}
